services:

  ds-notificacoes-api:
    build:
      context: .
      dockerfile: Dockerfile

    image: ds-notificacoes-api:1.0.0
    container_name: ds-notificacoes-api

    ports:
      - "${APP_PORT:-8080}:8080"

    environment:
      SERVER_PORT: "8080"

    restart: unless-stopped
